---
title: Entering motion
description: A set of legacy utilities to apply motion in your application.
order: 0
---

import SectionMessage from '@atlaskit/section-message';

import MotionFadeOutSingleElement from '../../examples/constellation/motion-fade-out-single-element';
import MotionSlideIn from '../../examples/constellation/motion-slide-in';
import MotionZoomIn from '../../examples/constellation/motion-zoom-in';
import MotionShrinkOut from '../../examples/constellation/motion-shrink-out';

Motion comes with entering motions out of the box. Most motions have a **pairing exiting motion**,
while some only have an exiting motion and no entering.

For consistency, don't try to mix and match. If an element **enters with one motion** it should
**leave with the same motion**.

## Fading in

`<FadeIn />` is useful for fading in one or more elements.

<Example
	highlight="5,19-21"
	packageName="@atlaskit/motion"
	Component={MotionFadeOutSingleElement}
	title="Single element"
/>

## Sliding in

`<SlideIn />` slide an element into position, generally used for things that appear from outside of
the viewport into view.

<Example
	highlight="7,37-52"
	packageName="@atlaskit/motion"
	Component={MotionSlideIn}
	title="Slide in element"
/>

## Zooming in

`<ZoomIn />` will over zoom an element into position.

<Example
	highlight="6,20-36"
	packageName="@atlaskit/motion"
	Component={MotionZoomIn}
	title="Zoom in elements"
/>

## Shrinking out

<SectionMessage appearance="warning">
	This motion only has an exiting motion. StaggeredEntrance will have no effect.
</SectionMessage>

`<ShrinkOut />` will shrink an element down to nothing when exiting. Works best with flex children
as collapsing margins can come with undesired behavior.

<Example
	highlight="6,29-57"
	packageName="@atlaskit/motion"
	Component={MotionShrinkOut}
	title="Shrink out elements"
/>

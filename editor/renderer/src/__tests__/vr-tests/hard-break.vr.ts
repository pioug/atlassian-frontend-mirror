import { snapshot } from '@af/visual-regression';

import { HardBreakParagraphRenderer, HardBreakListRenderer } from './hard-break.fixture.vr.ap';

snapshot(HardBreakParagraphRenderer);
snapshot(HardBreakListRenderer);

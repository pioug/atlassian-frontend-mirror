import { defineMessages } from 'react-intl-next';

export const chartPlaceholderMessages = defineMessages({
  chartPlaceholderText: {
    id: 'fabric.editor.confluence.legacyMobileMacros.chartPlaceholderText',
    defaultMessage:
      'A chart should be here, but it can’t be shown. Open this page in a browser to see the chart.',
    description:
      'Message shown in place of chart extension on mobile (where the extension is not yet supported)',
  },
});

---
title: Code block
description: A code block highlights an entire block of code and keeps the formatting.
order: 0
---

import CodeBlock from '../../examples/constellation/code-block';
import CodeBlockLineNumbers from '../../examples/constellation/code-block-line-numbers';
import CodeBlockLineHighlight from '../../examples/constellation/code-block-line-highlight';
import CodeBlockShouldWrapLongLinesExample from '../../examples/constellation/code-block-should-wrap-long-lines';

## Default

A code block highlights an entire block of code and keeps the formatting.

<Example Component={CodeBlock} packageName="@atlaskit/code" />

## Line numbers

You can display a code block with or without line numbers. Line numbers are included by default.

<Example Component={CodeBlockLineNumbers} packageName="@atlaskit/code" />

## Line highlighting

You can highlight lines in a code block.

- To highlight one line, input the line number: `highlight="3"`
- To highlight a group of lines, input the line numbers as a range: `highlight="1-5"`
- To highlight multiple groups, separate the individual lines and ranges with a comma: `highlight="1-5,7,10,15-20"`

<Example Component={CodeBlockLineHighlight} packageName="@atlaskit/code" />

## Wrapping long lines

By default, long lines will result in a horizontal-scrolling code block. You can use the `shouldWrapLongLines` prop to make the long lines wrap instead.

<Example
  Component={CodeBlockShouldWrapLongLinesExample}
  packageName="@atlaskit/code"
/>

# @atlaskit/cli-output

Shared, terminal-safe presentation primitives for Atlassian command-line tools. It provides semantic
text colors, a GitHub-dark-like code surface, and aligned structured-property tables while leaving
JSON rendering to each CLI's own stable contract.

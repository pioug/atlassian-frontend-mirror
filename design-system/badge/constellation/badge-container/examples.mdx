---
title: Badge container
description: A badge container keeps the style of a normal badge without the formatting.
order: 0
---

import BadgeContainerColor from '../../examples/constellation/badge-container-color';
import BadgeContainerTextColor from '../../examples/constellation/badge-container-text-color';

## Background color

Use `backgroundColor` to change the background color of the badge.

<Example Component={BadgeContainerColor} packageName="@atlaskit/badge" />

## Text color

Use `textColor` to change the text color of the badge.

<Example Component={BadgeContainerTextColor} packageName="@atlaskit/badge" />

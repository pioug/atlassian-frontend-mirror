---
title: Link icon button
description: An icon-only button that helps people navigate to a common link or page location.
order: 0
---

import SectionMessage from '@atlaskit/section-message';

import LinkIconButton from '../../examples/constellation/new-button/link-icon-button/link-icon-button';
import LinkIconButtonTooltip from '../../examples/constellation/new-button/link-icon-button/link-icon-button-tooltip';
import LinkIconButtonTooltipOptions from '../../examples/constellation/new-button/link-icon-button/link-icon-button-tooltip-options';
import LinkIconButtonPrimary from '../../examples/constellation/new-button/link-icon-button/link-icon-button-primary';
import LinkIconButtonSubtle from '../../examples/constellation/new-button/link-icon-button/link-icon-button-subtle';
import LinkIconButtonCircle from '../../examples/constellation/new-button/link-icon-button/link-icon-button-circle';
import LinkIconButtonIconOverrides from '../../examples/constellation/new-button/link-icon-button/link-icon-button-icon-overrides';
import LinkIconButtonWithRouting from '../../examples/constellation/new-button/link-icon-button/link-icon-button-with-routing';
import LinkIconButtonDisabled from '../../examples/constellation/new-button/link-icon-button/link-icon-button-disabled';
import LinkIconButtonSpacing from '../../examples/constellation/new-button/link-icon-button/link-icon-button-spacing';
import LinkIconButtonSelected from '../../examples/constellation/new-button/link-icon-button/link-icon-button-selected';

## Default

For buttons that are links for navigation rather than on-page actions. Link icon buttons render an
`<a>` tag instead of a `<button>`. They accept anchor tag HTML attributes, including `href`.

<Example Component={LinkIconButton} packageName="@atlaskit/button" />

## Routing

When available, link icon button will consume the router link component configured in the **app
provider**. This allows router links to be configured once across an entire application. (This
replaces [the component prop](/components/button/button-legacy/examples#routing)).

These links won't use the configured router link component when passed to `href`, and instead render
a standard `<a>` tag:

- External links
- Non-HTTP-based links (`mailto:`, `tel:`, `sms:`)
- Anchor / hash links (`#`)

If your application doesn't have an existing app provider or `routerLinkComponent` configuration,
see how to set this up in
[the app provider documentation](/components/app-provider/examples#router-links).

## Tooltips

<SectionMessage appearance="information">
	Tooltips are currently disabled by default. They will be enabled by default in a later release.
	Prefer using built-in tooltips over wrapping the link icon button manually.
</SectionMessage>

Set `isTooltipDisabled` to false to enable the tooltip. The value of `label` will be used for the
tooltip content.

<Example Component={LinkIconButtonTooltip} packageName="@atlaskit/button" />

### Overriding tooltip props

Use the `tooltip` prop to override the default tooltip props. It accepts all
[tooptip props](/components/tooltip/code), excluding `children`.

<Example Component={LinkIconButtonTooltipOptions} packageName="@atlaskit/button" />

## Appearance

### Primary

A primary button calls attention to the most important action on a page or in an area. Primary icon
buttons aren't common because critical actions should typically use
[a button with a label for clarity](/components/button).

<Example Component={LinkIconButtonPrimary} packageName="@atlaskit/button" />

### Subtle

Use a `subtle` icon button for secondary actions.

<Example Component={LinkIconButtonSubtle} packageName="@atlaskit/button" />

## Spacing

Icon button spacing depends on the surrounding UI. Default spacing is used for most use cases and
`compact` for tables or smaller spaces.

<Example Component={LinkIconButtonSpacing} packageName="@atlaskit/button" />

## Shape

### Circle

Only use circle icon buttons in the top navigation or other areas that already have circular
buttons.

<Example Component={LinkIconButtonCircle} packageName="@atlaskit/button" />

## Overriding icon props

Use the `icon` render prop to override the default icon props.

<Example Component={LinkIconButtonIconOverrides} packageName="@atlaskit/button" />

## Advanced href usage with TypeScript

Link button accepts a generic type containing the configured router link's props. This is only
required for advanced usage to allow an object to be passed to `href`.

<Example
	Component={LinkIconButtonWithRouting}
	packageName="@atlaskit/button"
	appearance="source-only"
	isCodeSandboxDisabled
/>

## States

### Disabled

The `disabled` HTML attribute does not exist for anchor `<a>` tags, so link buttons are disabled
using this accessible technique:

- Adds `aria-disabled="true"`
- Adds `role="link"`
- Removes `href` attribute

<Example Component={LinkIconButtonDisabled} packageName="@atlaskit/button" />

### Selected

Set `isSelected` to indicate the button is selected.

<Example Component={LinkIconButtonSelected} packageName="@atlaskit/button" />

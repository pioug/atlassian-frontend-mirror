/* eslint-disable @repo/internal/deprecations/deprecation-ticket-required -- VOLTC-139 tracks removal of these deprecated re-export shims. */

/**
 * @deprecated Use `import { getSelectedTableInfo } from '@atlaskit/editor-tables/utils'` instead.
 */
// eslint-disable-next-line @atlaskit/editor/no-re-export -- Compatibility shim for deprecated API
export { getSelectedTableInfo } from './get-selected-table-info';
/**
 * @deprecated Use `import { getSelectedCellInfo } from '@atlaskit/editor-tables/utils'` instead.
 */
// eslint-disable-next-line @atlaskit/editor/no-re-export -- Compatibility shim for deprecated API
export { getSelectedCellInfo } from './get-selected-cell-info';

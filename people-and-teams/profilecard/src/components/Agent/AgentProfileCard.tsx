import React from 'react';

import { type RovoAgent } from '../../types';

const AgentProfileCard = ({ agent }: { agent?: RovoAgent }) => {
	return <>ROVO AGENT PROFILE CARD</>;
};

export default AgentProfileCard;

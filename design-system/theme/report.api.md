## API Report File for "@atlaskit/theme"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

````ts
/// <reference types="react" />

import { ComponentType } from 'react';
import { FC } from 'react';
import { ReactNode } from 'react';

declare interface AKThemeProviderProps {
  mode?: ThemeModes;
  background?: ThemedValue<string>;
}

/**
 * @deprecated Please use `@atlaskit/visually-hidden`
 */
export declare const assistive: () => {
  border: string;
  clip: string;
  height: string;
  overflow: 'hidden';
  padding: string;
  position: 'absolute';
  width: string;
  whiteSpace: 'nowrap';
};

export declare interface AtlaskitThemeProps {
  theme: {
    __ATLASKIT_THEME__: Theme;
  };
  [index: string]: any;
}

/**
 * __Atlaskit Theme Provider__
 *
 * The global theme provider is used to provide theme context to an application.
 * It should be used once in application code at the root, or as near to the root as possible,
 * to inject the global theme via React context.
 *
 * - [Examples](https://atlaskit.atlassian.com/examples/design-system/theme)
 *
 * @example
 * ```jsx
 * import { AtlaskitThemeProvider } from '@atlaskit/theme/components';
 *
 * const App = () => (
 *  <AtlaskitThemeProvider mode="dark">
 *   <App />
 *  </AtlaskitThemeProvider>
 * );
 * ```
 */
export declare const AtlaskitThemeProvider: FC<AKThemeProviderProps>;

declare const B100 = '#4C9AFF';

declare const B200 = '#2684FF';

declare const B300 = '#0065FF';

declare const B400 = '#0052CC';

declare const B50 = '#DEEBFF';

declare const B500 = '#0747A6';

declare const B75 = '#B3D4FF';

declare const background: ThemedValue<'var(--ds-surface)'>;

declare const backgroundActive: ThemedValue<'var(--ds-background-selected)'>;

declare const backgroundHover: ThemedValue<'var(--ds-background-neutral-hovered)'>;

declare const backgroundOnLayer: ThemedValue<'var(--ds-surface-overlay)'>;

declare const blue: ThemedValue<string>;

export declare const borderRadius: () => number;

export declare const CHANNEL = '__ATLASKIT_THEME__';

declare const codeBlock: ThemedValue<string>;

export declare const codeFontFamily: () => string;

declare const colorPalette: (
  palette?: colorPaletteType,
) => {
  background: string;
  text: string;
}[];

declare const colorPalette16: {
  background: string;
  text: string;
}[];

declare const colorPalette24: {
  background: string;
  text: string;
}[];

declare const colorPalette8: {
  background: string;
  text: string;
}[];

declare namespace colorPalettes {
  export { colorPalette8, colorPalette16, colorPalette24, colorPalette };
}
export { colorPalettes };

export declare type colorPaletteType = '8' | '16' | '24';

declare namespace colors {
  export {
    R50,
    R75,
    R100,
    R200,
    R300,
    R400,
    R500,
    Y50,
    Y75,
    Y100,
    Y200,
    Y300,
    Y400,
    Y500,
    G50,
    G75,
    G100,
    G200,
    G300,
    G400,
    G500,
    B50,
    B75,
    B100,
    B200,
    B300,
    B400,
    B500,
    P50,
    P75,
    P100,
    P200,
    P300,
    P400,
    P500,
    T50,
    T75,
    T100,
    T200,
    T300,
    T400,
    T500,
    N0,
    N10,
    N20,
    N30,
    N40,
    N50,
    N60,
    N70,
    N80,
    N90,
    N100,
    N200,
    N300,
    N400,
    N500,
    N600,
    N700,
    N800,
    N900,
    N10A,
    N20A,
    N30A,
    N40A,
    N50A,
    N60A,
    N70A,
    N80A,
    N90A,
    N100A,
    N200A,
    N300A,
    N400A,
    N500A,
    N600A,
    N700A,
    N800A,
    DN900,
    DN800,
    DN700,
    DN600,
    DN500,
    DN400,
    DN300,
    DN200,
    DN100,
    DN90,
    DN80,
    DN70,
    DN60,
    DN50,
    DN40,
    DN30,
    DN20,
    DN10,
    DN0,
    DN800A,
    DN700A,
    DN600A,
    DN500A,
    DN400A,
    DN300A,
    DN200A,
    DN100A,
    DN90A,
    DN80A,
    DN70A,
    DN60A,
    DN50A,
    DN40A,
    DN30A,
    DN20A,
    DN10A,
    background,
    backgroundActive,
    backgroundHover,
    backgroundOnLayer,
    text,
    textHover,
    textActive,
    subtleText,
    placeholderText,
    heading,
    subtleHeading,
    codeBlock,
    link,
    linkHover,
    linkActive,
    linkOutline,
    primary,
    blue,
    teal,
    purple,
    red,
    yellow,
    green,
    skeleton,
  };
}
export { colors };

/**
 * createTheme is used to create a set of Providers and Consumers for theming components.
 * - Takes a default theme function; this theme function gets a set of props, and returns tokens
 *  based on those props. An example of this default theme function is one that produces the standard
 *  appearance of the component
 * - Returns three things - a Provider that allow for additional themes to be applied, a Consumer
 *  that can get the current theme and fetch it, and a custom hook - useTheme which provides an alternate (although functionally the same) API
 *  to the Consumer.
 */
export declare function createTheme<ThemeTokens, ThemeProps>(
  defaultGetTokens: GetThemeTokensFn<ThemeTokens, ThemeProps>,
): {
  Consumer: ComponentType<
    ThemeProps extends void
      ? ThemeConsumerFn<ThemeTokens>
      : ThemeConsumerFn<ThemeTokens> & ThemeProps
  >;
  Provider: ComponentType<{
    children?: ReactNode;
    value?: ThemeProp<ThemeTokens, ThemeProps>;
  }>;
  useTheme: GetThemeTokensFn<ThemeTokens, ThemeProps>;
};

export declare interface CustomThemeProps {
  theme: Theme;
  [index: string]: any;
}

declare const _default: {
  Provider: ComponentType<{
    children?: ReactNode;
    value?: ThemeProp<GlobalThemeTokens, void> | undefined;
  }>;
  Consumer: ComponentType<{
    children: (tokens: GlobalThemeTokens) => ReactNode;
  }>;
};
export default _default;

export declare const DEFAULT_THEME_MODE = 'light';

export declare type DefaultValue = string | number;

declare const DN0 = '#0D1424';

declare const DN10 = '#0E1624';

declare const DN100 = '#67758F';

declare const DN100A = 'rgba(13, 20, 36, 0.53)';

declare const DN10A = 'rgba(13, 20, 36, 0.97)';

declare const DN20 = '#121A29';

declare const DN200 = '#7988A3';

declare const DN200A = 'rgba(13, 20, 36, 0.47)';

declare const DN20A = 'rgba(13, 20, 36, 0.95)';

declare const DN30 = '#1B2638';

declare const DN300 = '#8C9CB8';

declare const DN300A = 'rgba(13, 20, 36, 0.40)';

declare const DN30A = 'rgba(13, 20, 36, 0.92)';

declare const DN40 = '#202B3D';

declare const DN400 = '#9FB0CC';

declare const DN400A = 'rgba(13, 20, 36, 0.36)';

declare const DN40A = 'rgba(13, 20, 36, 0.89)';

declare const DN50 = '#283447';

declare const DN500 = '#ABBBD6';

declare const DN500A = 'rgba(13, 20, 36, 0.29)';

declare const DN50A = 'rgba(13, 20, 36, 0.85)';

declare const DN60 = '#313D52';

declare const DN600 = '#B8C7E0';

declare const DN600A = 'rgba(13, 20, 36, 0.18)';

declare const DN60A = 'rgba(13, 20, 36, 0.81)';

declare const DN70 = '#3B475C';

declare const DN700 = '#CED9EB';

declare const DN700A = 'rgba(13, 20, 36, 0.14)';

declare const DN70A = 'rgba(13, 20, 36, 0.78)';

declare const DN80 = '#455166';

declare const DN800 = '#DCE5F5';

declare const DN800A = 'rgba(13, 20, 36, 0.06)';

declare const DN80A = 'rgba(13, 20, 36, 0.73)';

declare const DN90 = '#56637A';

declare const DN900 = '#E6EDFA';

declare const DN90A = 'rgba(13, 20, 36, 0.63)';

declare const e100: ThemedValue<string>;

declare const e200: ThemedValue<string>;

declare const e300: ThemedValue<string>;

declare const e400: ThemedValue<string>;

declare const e500: ThemedValue<string>;

export declare type Elevation = 'e100' | 'e200' | 'e300' | 'e400' | 'e500';

declare namespace elevation {
  export { e100, e200, e300, e400, e500 };
}
export { elevation };

/**
 * @deprecated Please use `@atlaskit/focus-ring`
 */
export declare const focusRing: (
  color?: string,
  outlineWidth?: number,
) => string;

export declare const fontFamily: () => string;

export declare const fontSize: () => number;

export declare const fontSizeSmall: () => number;

declare const G100 = '#79F2C0';

declare const G200 = '#57D9A3';

declare const G300 = '#36B37E';

declare const G400 = '#00875A';

declare const G50 = '#E3FCEF';

declare const G500 = '#006644';

declare const G75 = '#ABF5D1';

export declare function getTheme(props?: ThemeProps): Theme;

declare type GetThemeTokensFn<ThemeTokens, ThemeProps> = (
  props: ThemeProps,
) => ThemeTokens;

export declare interface GlobalThemeTokens extends Theme {}

declare const green: ThemedValue<string>;

export declare const gridSize: () => number;

declare const h100: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text-subtlest)';
  fontWeight: number;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h200: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text-subtlest)';
  fontWeight: number;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h300: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  marginTop: string;
  textTransform: 'uppercase';
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h400: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h500: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h600: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h700: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h800: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const h900: (
  props?: ThemeProps,
) => {
  color: 'var(--ds-text)';
  fontWeight: number;
  letterSpacing: string;
  marginTop: string;
  fontSize: string;
  fontStyle: string;
  lineHeight: number;
};

declare const heading: ThemedValue<'var(--ds-text)'>;

declare const headingSizes: {
  h900: {
    size: number;
    lineHeight: number;
  };
  h800: {
    size: number;
    lineHeight: number;
  };
  h700: {
    size: number;
    lineHeight: number;
  };
  h600: {
    size: number;
    lineHeight: number;
  };
  h500: {
    size: number;
    lineHeight: number;
  };
  h400: {
    size: number;
    lineHeight: number;
  };
  h300: {
    size: number;
    lineHeight: number;
  };
  h200: {
    size: number;
    lineHeight: number;
  };
  h100: {
    size: number;
    lineHeight: number;
  };
};

declare interface Layers {
  card: 100;
  navigation: 200;
  dialog: 300;
  layer: 400;
  blanket: 500;
  modal: 510;
  flag: 600;
  spotlight: 700;
  tooltip: 800;
}

export declare const layers: {
  [P in keyof Layers]: () => Layers[P];
};

declare const link: ThemedValue<'var(--ds-link)'>;

declare const linkActive: ThemedValue<'var(--ds-link-pressed)'>;

declare const linkHover: ThemedValue<'var(--ds-link-pressed)'>;

declare const linkOutline: ThemedValue<'var(--ds-border-focused)'>;

declare type Modes<V> = {
  [key in ThemeModes]?: V;
};

declare const N0 = '#FFFFFF';

declare const N10 = '#FAFBFC';

declare const N100 = '#7A869A';

declare const N100A = 'rgba(9, 30, 66, 0.54)';

declare const N10A = 'rgba(9, 30, 66, 0.02)';

declare const N20 = '#F4F5F7';

declare const N200 = '#6B778C';

declare const N200A = 'rgba(9, 30, 66, 0.60)';

declare const N20A = 'rgba(9, 30, 66, 0.04)';

declare const N30 = '#EBECF0';

declare const N300 = '#5E6C84';

declare const N300A = 'rgba(9, 30, 66, 0.66)';

declare const N30A = 'rgba(9, 30, 66, 0.08)';

declare const N40 = '#DFE1E6';

declare const N400 = '#505F79';

declare const N400A = 'rgba(9, 30, 66, 0.71)';

declare const N40A = 'rgba(9, 30, 66, 0.13)';

declare const N50 = '#C1C7D0';

declare const N500 = '#42526E';

declare const N500A = 'rgba(9, 30, 66, 0.77)';

declare const N50A = 'rgba(9, 30, 66, 0.25)';

declare const N60 = '#B3BAC5';

declare const N600 = '#344563';

declare const N600A = 'rgba(9, 30, 66, 0.82)';

declare const N60A = 'rgba(9, 30, 66, 0.31)';

declare const N70 = '#A5ADBA';

declare const N700 = '#253858';

declare const N700A = 'rgba(9, 30, 66, 0.89)';

declare const N70A = 'rgba(9, 30, 66, 0.36)';

declare const N80 = '#97A0AF';

declare const N800 = '#172B4D';

declare const N800A = 'rgba(9, 30, 66, 0.95)';

declare const N80A = 'rgba(9, 30, 66, 0.42)';

declare const N90 = '#8993A4';

declare const N900 = '#091E42';

declare const N90A = 'rgba(9, 30, 66, 0.48)';

/**
 * @deprecated Please use `@atlaskit/focus-ring`
 */
export declare const noFocusRing: () => string;

export declare interface NoThemeProps {
  [index: string]: any;
}

declare const P100 = '#998DD9';

declare const P200 = '#8777D9';

declare const P300 = '#6554C0';

declare const P400 = '#5243AA';

declare const P50 = '#EAE6FF';

declare const P500 = '#403294';

declare const P75 = '#C0B6F2';

declare const placeholderText: ThemedValue<'var(--ds-text-subtlest)'>;

declare const primary: ThemedValue<'var(--ds-background-brand-bold)'>;

declare const purple: ThemedValue<string>;

declare const R100 = '#FF8F73';

declare const R200 = '#FF7452';

declare const R300 = '#FF5630';

declare const R400 = '#DE350B';

declare const R50 = '#FFEBE6';

declare const R500 = '#BF2600';

declare const R75 = '#FFBDAD';

declare const red: ThemedValue<string>;

declare const skeleton: () => 'var(--ds-skeleton)';

/**
 * These styles are mirrored in:
 * packages/design-system/menu/src/internal/components/skeleton-shimmer.tsx
 *
 * Please update both.
 */
export declare const skeletonShimmer: () => {
  readonly css: {
    readonly backgroundColor: 'var(--ds-skeleton)';
    readonly animationDuration: '1.5s';
    readonly animationIterationCount: 'infinite';
    readonly animationTimingFunction: 'linear';
    readonly animationDirection: 'alternate';
  };
  readonly keyframes: {
    readonly from: {
      readonly backgroundColor: 'var(--ds-skeleton)';
    };
    readonly to: {
      readonly backgroundColor: 'var(--ds-skeleton-subtle)';
    };
  };
};

declare const subtleHeading: ThemedValue<'var(--ds-text-subtlest)'>;

declare const subtleText: ThemedValue<'var(--ds-text-subtlest)'>;

declare const T100 = '#79E2F2';

declare const T200 = '#00C7E6';

declare const T300 = '#00B8D9';

declare const T400 = '#00A3BF';

declare const T50 = '#E6FCFF';

declare const T500 = '#008DA6';

declare const T75 = '#B3F5FF';

declare const teal: ThemedValue<string>;

declare const text: ThemedValue<'var(--ds-text)'>;

declare const textActive: ThemedValue<'var(--ds-text-selected)'>;

declare const textHover: ThemedValue<'var(--ds-text)'>;

export declare interface Theme {
  mode: ThemeModes;
}

export declare const THEME_MODES: string[];

declare type ThemeConsumerFn<ThemeTokens> = {
  children: (tokens: ThemeTokens) => ReactNode;
};

export declare function themed<V = DefaultValue>(
  modesOrVariant: Modes<V> | string,
  variantModes?: VariantModes<V>,
): ThemedValue<V>;

export declare type ThemedValue<V = DefaultValue> = (
  props?: ThemeProps,
) => V | '';

export declare type ThemeModes = 'light' | 'dark';

export declare type ThemeProp<ThemeTokens, ThemeProps> = (
  getTokens: GetThemeTokensFn<ThemeTokens, ThemeProps>,
  themeProps: ThemeProps,
) => ThemeTokens;

export declare type ThemeProps =
  | AtlaskitThemeProps
  | CustomThemeProps
  | NoThemeProps;

declare namespace typography {
  export { headingSizes, h900, h800, h700, h600, h500, h400, h300, h200, h100 };
}
export { typography };

export declare const useGlobalTheme: (props: void) => GlobalThemeTokens;

declare type VariantModes<V> = {
  [index: string]: Modes<V>;
};

/**
 * @deprecated Please use `@atlaskit/visually-hidden`
 */
export declare const visuallyHidden: () => {
  border: string;
  clip: string;
  height: string;
  overflow: 'hidden';
  padding: string;
  position: 'absolute';
  width: string;
  whiteSpace: 'nowrap';
};

declare const Y100 = '#FFE380';

declare const Y200 = '#FFC400';

declare const Y300 = '#FFAB00';

declare const Y400 = '#FF991F';

declare const Y50 = '#FFFAE6';

declare const Y500 = '#FF8B00';

declare const Y75 = '#FFF0B3';

declare const yellow: ThemedValue<string>;

export {};
````

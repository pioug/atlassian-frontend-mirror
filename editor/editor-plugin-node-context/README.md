# Editor plugin node context

Provides opt-in editor actions for resolving viewport points and rectangles to
ADF node context. Add `nodeContextPlugin` to an editor preset, then call the
actions through `editorApi.nodeContext`.

---
order: 0
---

import PageHeaderDefault from '../../examples/constellation/page-header-default';
import PageHeaderComplex from '../../examples/constellation/page-header-complex';
import PageHeaderCustomTitle from '../../examples/constellation/page-header-custom-title';
import PageHeaderFocusHeading from '../../examples/constellation/page-header-focus-heading';

## Default

Use a default page header for a title underneath [breadcrumbs](/components/breadcrumbs/examples).

The header automatically wraps if the text is too long.

<Example Component={PageHeaderDefault} packageName="@atlaskit/page-header" />

## Complex

Page headers can include [breadcrumbs](/components/breadcrumbs/examples), which appear above the header. You can also pass [buttons](/components/button/examples) into `actions`. The action buttons appear at the end of the header.

To help people refine the page content on a more granular level, a [filter bar](/components/textfield/examples) and [search dropdown](/components/dropdown-menu/examples) component can be added to the `bottomBar` below the header title.

<Example Component={PageHeaderComplex} packageName="@atlaskit/page-header" />

## Custom title component

Use custom components for titles where necessary. For example, an [inline edit](/components/inline-edit/examples) lets people edit the title on the page.

<Example
  Component={PageHeaderCustomTitle}
  packageName="@atlaskit/page-header"
/>

## Focus heading

You can set the focus back to the title for accessibility purposes.

<Example
  Component={PageHeaderFocusHeading}
  packageName="@atlaskit/page-header"
/>

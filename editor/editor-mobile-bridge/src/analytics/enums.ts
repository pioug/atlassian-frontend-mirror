export enum ActionSubject {
  EDITOR = 'editor',
  RENDERER = 'renderer',
}

export enum ActionSubjectID {
  COLLAB = 'collab',
}

export enum EventType {
  TRACK = 'track',
  OPERATIONAL = 'operational',
}

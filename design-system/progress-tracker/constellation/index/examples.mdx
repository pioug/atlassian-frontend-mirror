---
order: 0
---

import ProgressTrackerDefault from '../../examples/progressTrackerDefault';
import ProgressTrackerSpacingComfortable from '../../examples/progressTrackerSpacingComfortable';
import ProgressTrackerSpacingCosy from '../../examples/progressTrackerSpacingCosy';
import ProgressTrackerSpacingCompact from '../../examples/progressTrackerSpacingCompact';
import ProgressTrackerCompleted from '../../examples/completed';

## Default

The default version of progress tracker that shows all the steps and states in the journey.

The progress tracker component, takes an items prop, which is an ordered list of all steps in the journey.

<Example Component={ProgressTrackerDefault} componentName="@atlaskit/progress-tracker" />

## Spacing

Margin spacing type between steps.

### Comfortable

<Example Component={ProgressTrackerSpacingComfortable} componentName="@atlaskit/progress-tracker" />

### Cosy

<Example Component={ProgressTrackerSpacingCosy} componentName="@atlaskit/progress-tracker" />

### Compact

<Example Component={ProgressTrackerSpacingCompact} componentName="@atlaskit/progress-tracker" />

## Completed

A progress tracker that shows all steps have been completed.

<Example Component={ProgressTrackerCompleted} componentName="@atlaskit/progress-tracker" />

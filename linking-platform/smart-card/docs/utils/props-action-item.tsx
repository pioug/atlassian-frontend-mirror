import { BaseActionItem } from '../../src/view/FlexibleCard/components/blocks/types';

/**
 * Hack to display Flexible Smart Links `ActionItem` for documentation purpose.
 */
export default (props: BaseActionItem) => null;

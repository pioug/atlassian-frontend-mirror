---
order: 3
---

import buttonExample from '../../examples/constellation/token-button';
import modalExample from '../../examples/constellation/token-modal';
import tagExample from '../../examples/constellation/token-tag';
import tableExample from '../../examples/constellation/token-table';

import lineChartExample from '../../examples/constellation/token-line-chart';
import pieChartExample from '../../examples/constellation/token-pie-chart';
import barChartExample from '../../examples/constellation/token-bar-chart';

import TokensRadiusExample from '../../examples/constellation/token-radius';

import neutralExample from '../../examples/constellation/token-card-neutral';
import brandExample from '../../examples/constellation/token-card-brand';
import informationExample from '../../examples/constellation/token-card-information';
import successExample from '../../examples/constellation/token-card-success';
import warningExample from '../../examples/constellation/token-card-warning';
import dangerExample from '../../examples/constellation/token-card-danger';
import discoveryExample from '../../examples/constellation/token-card-discovery';
import selectedExample from '../../examples/constellation/token-card-selected';

import elevationExample from '../../examples/constellation/token-elevation';

import negativeSpaceExample from '../../examples/constellation/token-negative-space';

## Component examples

### Button

This primary button uses a background token, text token, and other tokens to create a brand appearance.

Buttons and other components can have multiple possible [interaction states](/foundations/color-new/#interaction-states), which require specific tokens to be applied for each state.

<Example
  Component={buttonExample.example}
  source={buttonExample.code}
  packageName="@atlaskit/tokens"
/>

### Modal dialog

Modal dialogs use a surface token and shadow token to illustrate [elevation](/foundations/elevation). The surface token ensures no content shows underneath the modal.

<Example
  Component={modalExample.example}
  source={modalExample.code}
  packageName="@atlaskit/tokens"
/>

### Tag

In the tag example, we can see the use of accent colors. Accent colors are available for text, icons, borders, and backgrounds. Apply accent tokens where [colors](/foundations/color-new/#color-roles) do not have a specific meaning tied to them.

<Example
  Component={tagExample.example}
  source={tagExample.code}
  packageName="@atlaskit/tokens"
/>

### Table

An overflow shadow can be applied on horizontal scrollable tables to indicate there is hidden content that can be scrolled back into view.

<Example
  Component={tableExample.example}
  source={tableExample.code}
  packageName="@atlaskit/tokens"
/>

## Chart examples

Use chart color tokens for chart elements that represent the data. Read our [data visualization color guideline](/foundations/color-new/data-visualization-color/) for guidance on color usage in charts.

### Line chart

<Example
  Component={lineChartExample.example}
  source={lineChartExample.code}
  packageName="@atlaskit/tokens"
/>

### Pie chart

<Example
  Component={pieChartExample.example}
  source={pieChartExample.code}
  packageName="@atlaskit/tokens"
/>

### Bar chart

<Example
  Component={barChartExample.example}
  source={barChartExample.code}
  packageName="@atlaskit/tokens"
/>

## Border examples

### Radius

Use the default radius of `border.radius` if in doubt. If you are nesting elements, ensure the outer element has the largest radius.

<TokensRadiusExample />

## Pairing examples

These pairing examples are some ways you can mix and match our tokens. If you decide to use a combination of tokens that’s not listed here, make sure to check the contrast to ensure it passes [WCAG AA guidelines](https://www.w3.org/WAI/WCAG2AA-Conformance).

### Neutral

<Example
  Component={neutralExample.example}
  source={neutralExample.code}
  packageName="@atlaskit/tokens"
/>

### Brand

<Example
  Component={brandExample.example}
  source={brandExample.code}
  packageName="@atlaskit/tokens"
/>

### Information

<Example
  Component={informationExample.example}
  source={informationExample.code}
  packageName="@atlaskit/tokens"
/>

### Success

<Example
  Component={successExample.example}
  source={successExample.code}
  packageName="@atlaskit/tokens"
/>

### Warning

<Example
  Component={warningExample.example}
  source={warningExample.code}
  packageName="@atlaskit/tokens"
/>

### Danger

<Example
  Component={dangerExample.example}
  source={dangerExample.code}
  packageName="@atlaskit/tokens"
/>

### Discovery

<Example
  Component={discoveryExample.example}
  source={discoveryExample.code}
  packageName="@atlaskit/tokens"
/>

### Selected

<Example
  Component={selectedExample.example}
  source={selectedExample.code}
  packageName="@atlaskit/tokens"
/>

### Elevations

There are four available elevations: default, raised, overlay and sunken.
Raised and overlay elevations require a combination of a surface color, and a matching shadow.

<Example
  Component={elevationExample.example}
  source={elevationExample.code}
  packageName="@atlaskit/tokens"
/>

## Spacing examples

### Negative values

Occasionally you may need a negative space value, for example a negative margin breaking content out of its container.
In this situation, use the CSS `calc` function to multiply a space token by `-1`.

**Note:**
Avoid performing any other calculations on space tokens, such as adding, subtracting, or dividing.
Avoid multiplying space tokens by values other than `-1`. The new [spacing scale](/components/tokens/all-tokens) provides a range of space values to suit many use cases.

<Example
  Component={negativeSpaceExample.example}
  source={negativeSpaceExample.code}
  packageName="@atlaskit/tokens"
/>

// THIS FILE IS GENERATED. DO NOT MODIFY IT MANUALLY.
export {};

describe('floating-toolbar wrapper', () => {
  it('check ./floating-toolbar exports all the same variables as the original', () => {
    const original = require('@atlaskit/editor-plugin-floating-toolbar');
    const wrapper = require('../floating-toolbar/index');
    const originalKeys = Object.keys(original).sort();
    const wrapperKeys = Object.keys(wrapper).sort();
    expect(originalKeys).toEqual(wrapperKeys);
  });
});

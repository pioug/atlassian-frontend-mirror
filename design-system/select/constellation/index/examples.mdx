---
order: 0
---

import SelectAsyncWithCallback from '../../examples/constellation/SelectAsyncWithCallback';
import SelectCreatable from '../../examples/constellation/SelectCreatable';
import SelectSingle from '../../examples/constellation/SelectSingle';
import SelectMulti from '../../examples/constellation/SelectMulti';
import SelectRadio from '../../examples/constellation/SelectRadio';
import SelectCheckbox from '../../examples/constellation/SelectCheckbox';
import SelectPopup from '../../examples/constellation/SelectPopup';

## Single select

Allows the user to select a single item from a dropdown menu.

<Example Component={SelectSingle} componentName="@atlaskit/select" />

## Multi select

Allows the user to select a multiple items from a dropdown menu.

<Example Component={SelectMulti} componentName="@atlaskit/select" />

## Named exports

To consolidate patterns and improve developer experience Select provides some pre-configured components as named exports.

### Radio select

A select with inline radio elements in the options.

<Example Component={SelectRadio} componentName="@atlaskit/select" />

### Checkbox select

A select with inline checkbox elements in the options displayed in the menu.
Use this select when displaying selected multi select options is imperative, but there may not be enough real-estate in the control.

<Example Component={SelectCheckbox} componentName="@atlaskit/select" />

### Creatable select

Allows the user to insert an item into the list for selection.

<Example Component={SelectCreatable} componentName="@atlaskit/select" />

## Popup select

A searchable select where the input is detached from the trigger. Use this select in scenarios where the trigger needs a custom appearance, or must occupy very limited horizontal space.

<Example Component={SelectPopup} componentName="@atlaskit/select" />

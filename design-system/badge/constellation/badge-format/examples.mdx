---
title: Badge format
description: A badge format is used to compose your own badge.
order: 0
---

import BadgeFormatDefault from '../../examples/constellation/badge-format-default';
import BadgeFormatMaxValue from '../../examples/constellation/badge-format-max-value';

## Default

The default form of a badge format.

<Example Component={BadgeFormatDefault} packageName="@atlaskit/badge" />

## Max value

Use `max` to cap the value of a composed badge.
When the value to display is greater than the `max`,
a `+` will automatically be added to that value.

<Example Component={BadgeFormatMaxValue} packageName="@atlaskit/badge" />

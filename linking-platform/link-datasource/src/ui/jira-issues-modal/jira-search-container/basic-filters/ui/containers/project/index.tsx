import React from 'react';

import BasicFilterDropdown from '../../common/dropdown';

const ProjectFilterContainer = () => {
  return <BasicFilterDropdown />;
};

export default ProjectFilterContainer;

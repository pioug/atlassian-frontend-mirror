---
title: Icon button
description:
  An icon-only button lets people take a common and recognizable action where space is limited.
order: 1
props:
  packageName: '@atlaskit/button/new'
  exports:
    - IconButton
---

## Props

Each button type also supports its respective HTML element attributes, except for:

- `disabled` (use `isDisabled` instead)
- `style`
- `role`

<TSMorphProps exportName="IconButton" packageName="@atlaskit/button/new" />

---
title: Menu group
description: A generic menu group for rendering menus within navigation elements.
order: 0
---

import MenuDefault from '../../examples/constellation/menu-default';
import MenuDensity from '../../examples/constellation/menu-density';

## Default

`MenuGroup` is one of the two available group components. This menu is designed to be placed inline inside of navigation elements.

If composing your menu as a popup, you'll want to use [PopupMenuGroup](/components/menu/popup-menu-group/examples), which constrains its width to predefined values.

<Example Component={MenuDefault} packageName="@atlaskit/menu" />

## Density

A `MenuGroup` can be configured to accept different spacing values. Applying a `compact` spacing value
will trim gutters and whitespace for higher density content.

<Example Component={MenuDensity} packageName="@atlaskit/menu" />

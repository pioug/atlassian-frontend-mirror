---
title: Breadcrumbs
description: Use breadcrumbs to show a user’s location in a site or app.
order: 0
---

import BreadcrumbsRefreshDefault from '../../examples/constellation/breadcrumbs-refresh-default';
import BreadcrumbsRefreshCurrentItem from '../../examples/constellation/breadcrumbs-refresh-current-item';
import BreadcrumbsRefreshElemBefore from '../../examples/constellation/breadcrumbs-refresh-elem-before';
import BreadcrumbsRefreshSizeSmall from '../../examples/constellation/breadcrumbs-refresh-size-small';
import BreadcrumbsRefreshOverflow from '../../examples/constellation/breadcrumbs-refresh-overflow';
import BreadcrumbsRefreshSkeleton from '../../examples/constellation/breadcrumbs-refresh-skeleton';

## Default

The default form of breadcrumbs shows where a page sits in a hierarchy. Each item links to a parent
page.

<Example
	Component={BreadcrumbsRefreshDefault}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

## Current item

Use `BreadcrumbsCurrentItem` for the current page when it adds information the page title lacks,
such as a Jira work item key. The current item always exposes a copy-link button on hover that
copies its `href` to the clipboard, so provide a valid `href` for the current page.

<Example
	Component={BreadcrumbsRefreshCurrentItem}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

## Elements before items

Use `elemBefore` to display an element such as an icon, logo, or tile before a breadcrumb item.

<Example
	Component={BreadcrumbsRefreshElemBefore}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

## Size

Use `size="small"` for dense surfaces such as side panels, where the default size would crowd
surrounding content.

<Example
	Component={BreadcrumbsRefreshSizeSmall}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

## Overflow

When the breadcrumb trail is too long for the available width, the middle items collapse into an
overflow menu. Select the ellipsis to reveal the collapsed items.

<Example
	Component={BreadcrumbsRefreshOverflow}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

## Skeleton

While data is loading, render `BreadcrumbsSkeleton` with `BreadcrumbsSkeletonItem` placeholders in
place of the real breadcrumbs, then swap to `Breadcrumbs` once the data is ready.

<Example
	Component={BreadcrumbsRefreshSkeleton}
	packageName="@atlaskit/breadcrumbs"
	featureFlagDefaults={{ 'platform_dst_breadcrumbs-refresh': true }}
/>

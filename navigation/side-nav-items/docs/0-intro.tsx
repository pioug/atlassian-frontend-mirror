import { md } from '@atlaskit/docs';

// This is used by the website generator to define which components are tabs, and the tab order.
// If this export is not present, tabs are generated in case-sensitive alphabetical-order (not source-code order).
export const _PageTabs = ['Examples'];

export const Examples: JSX.Element = md`
	Currently documented on the
	[Atlassian Design System website](https://atlassian.design/components/navigation-system/side-nav-items/examples).

	The documentation will move here in the future.
`;

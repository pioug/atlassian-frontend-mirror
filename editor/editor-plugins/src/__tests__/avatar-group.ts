// THIS FILE IS GENERATED. DO NOT MODIFY IT MANUALLY.
export {};

describe('avatar-group wrapper', () => {
    it('check ./avatar-group exports all the same variables as the original', () => {
        const original = require('@atlaskit/editor-plugin-avatar-group');
        const wrapper = require('../avatar-group/index');
        const originalKeys = Object.keys(original).sort();
        const wrapperKeys = Object.keys(wrapper).sort();
        expect(originalKeys).toEqual(wrapperKeys);
    });
});

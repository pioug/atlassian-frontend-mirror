---
order: 0
title: Avatar tag
description:
  An avatar tag represents individuals, teams, or AI agents for tagging, quick recognition and
  navigation.
---

import SectionMessage from '@atlaskit/section-message';
import AvatarTagDefault from '../../examples/constellation/avatar-tag-default';
import AvatarTagLink from '../../examples/constellation/avatar-tag-link';
import AvatarTagRemovable from '../../examples/constellation/avatar-tag-removable';
import AvatarTagTeam from '../../examples/constellation/avatar-tag-team';
import AvatarTagTeamLink from '../../examples/constellation/avatar-tag-team-link';
import AvatarTagTeamRemovable from '../../examples/constellation/avatar-tag-team-removable';
import AvatarTagTeamVerified from '../../examples/constellation/avatar-tag-team-verified';
import AvatarTagAgent from '../../examples/constellation/avatar-tag-agent';
import AvatarTagAgentLink from '../../examples/constellation/avatar-tag-agent-link';
import AvatarTagAgentRemovable from '../../examples/constellation/avatar-tag-agent-removable';
import AvatarTagMaxWidth from '../../examples/constellation/avatar-tag-max-width';

<SectionMessage title="Motion in Early Access" appearance="warning">
	Motion has been updated in Avatar tag and is in Early Access. The motion updates are currently
	behind the feature flag: platform-dst-motion-uplift-labels.
</SectionMessage>

## User (Default)

The default form of an avatar tag for users, with `text` and an `avatar` component. User avatar tags
use a rounded pill design and are used to label individuals for quick recognition.

<Example
	Component={AvatarTagDefault}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## User (Link)

A user avatar tag with an `href` can link to more information about the person.

<Example
	Component={AvatarTagLink}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## User (Non-removable)

Set `isRemovable` to false to prevent users from removing the user avatar tag. Avatar tags are
removable by default.

<Example
	Component={AvatarTagRemovable}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Team (Default)

Team avatar tags use a square design with rounded corners, similar to regular tags. They are used to
represent teams or groups with a square team avatar.

<Example
	Component={AvatarTagTeam}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Team (Link)

A team avatar tag with an `href` can link to more information about the team.

<Example
	Component={AvatarTagTeamLink}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Team (Non-removable)

Set `isRemovable` to false to prevent users from removing the team avatar tag.

<Example
	Component={AvatarTagTeamRemovable}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Team (Verified)

Set `isVerified` to true to display a verified badge after the team name. This is only available for
team/other type avatar tags.

<Example
	Component={AvatarTagTeamVerified}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Agent (Default)

Agent avatar tags use a hexagonal design to represent AI agents like Rovo. They help distinguish
AI-powered assistants from users and teams.

<Example
	Component={AvatarTagAgent}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Agent (Link)

An agent avatar tag with an `href` can link to more information about the agent.

<Example
	Component={AvatarTagAgentLink}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Agent (Non-removable)

Set `isRemovable` to false to prevent users from removing the agent avatar tag.

<Example
	Component={AvatarTagAgentRemovable}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Max width

Use `maxWidth` to control the maximum width of the avatar tag. When the tag exceeds this width, it
will be truncated with an ellipsis.

<Example
	Component={AvatarTagMaxWidth}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

---
title: Custom icon
description: A custom icon is for icons which have a very specific or unique action.
order: 0
---

import CustomIconDefault from '../../examples/constellation/custom-icon-default.tsx';
import CustomIconSmall from '../../examples/constellation/custom-icon-small.tsx';
import CustomIconMedium from '../../examples/constellation/custom-icon-medium.tsx';
import CustomIconLarge from '../../examples/constellation/custom-icon-large.tsx';
import CustomIconPrimaryColor from '../../examples/constellation/custom-icon-primary-color.tsx';
import CustomIconSecondaryColor from '../../examples/constellation/custom-icon-secondary-color.tsx';

## Default

By default, custom icons are displayed in the `medium` size.

<Example Component={CustomIconDefault} packageName="@atlaskit/icon" />

## Sizing

### Small

The `small` icon size (16px) is used when space is at a premium, such as search results or inline content.

<Example Component={CustomIconSmall} packageName="@atlaskit/icon" />

### Medium

The `medium` icon size (24px) is used in common interfaces.

<Example Component={CustomIconMedium} packageName="@atlaskit/icon" />

### Large

The `large` icon size (32px) is used sparingly to emphasize a concept or when there’s a lot of space.

<Example Component={CustomIconLarge} packageName="@atlaskit/icon" />

## Color

### Primary color

This refers to the main color of the icon. The icon’s primary color inherits the current font color by default.

<Example Component={CustomIconPrimaryColor} packageName="@atlaskit/icon" />

### Secondary color

The icon’s secondary color inherits the page background for an icon that supports two colors by default.

<Example Component={CustomIconSecondaryColor} packageName="@atlaskit/icon" />

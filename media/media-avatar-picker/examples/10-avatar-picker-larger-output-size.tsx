import React from 'react';
import StatefulAvatarPickerDialog from '../example-helpers/StatefulAvatarPickerDialog';

export default () => (
	<StatefulAvatarPickerDialog placeholder={<div>Loading...</div>} outputSize={512} />
);

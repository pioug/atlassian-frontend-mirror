// This file is used as a "clean slate" version of the api for reporting
// we don't want the api report for this to change every time someone adds/removes a feature flag

export type BooleanFeatureFlagType = string;

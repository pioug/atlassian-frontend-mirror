import { INPUT_METHOD } from '@atlaskit/editor-common/analytics';

export enum InputSource {
	TOOLBAR = INPUT_METHOD.TOOLBAR,
	KEYBOARD = INPUT_METHOD.KEYBOARD,
	PAGE_OVERFLOW_MENU = INPUT_METHOD.PAGE_OVERFLOW_MENU,
}

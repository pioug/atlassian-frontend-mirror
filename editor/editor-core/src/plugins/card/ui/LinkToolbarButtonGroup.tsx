import React from 'react';

export interface LinkToolbarButtonGroupProps {}

const LinkToolbarButtonGroup = ({}: LinkToolbarButtonGroupProps) => {
  return <span></span>;
};

export default LinkToolbarButtonGroup;

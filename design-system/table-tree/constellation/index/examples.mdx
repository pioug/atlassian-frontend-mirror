---
order: 0
---

import BasicExample from '../../examples/constellation/basic';
import AdvancedExample from '../../examples/constellation/advanced';
import UncontrolledExample from '../../examples/constellation/uncontrolled';
import ControlledExample from '../../examples/constellation/controlled';
import CustomChildExample from '../../examples/constellation/custom-child-component';
import LoadingStateRootExample from '../../examples/constellation/loading-state-root';
import LoadingStateNestedExample from '../../examples/constellation/loading-state-nested';
import AsyncAppendExample from '../../examples/constellation/async-append';
import AsyncUpdateExample from '../../examples/constellation/async-update';

## Basic

Table tree displays a table with expandable nested rows. The basic table tree has configurable props for header titles,
columns, column widths, and an array of table items (with child items).

The `content` of each item is customizable, but your data must otherwise conform to the expected schema.

<Example Component={BasicExample} packageName="@atlaskit/table-tree" />

## Advanced

For more advanced usage, you can compose `Cell`, `Header`, `Headers`, `Row`,
and `Rows` components.

This approach allows you to structure your data however you wish.

<Example Component={AdvancedExample} packageName="@atlaskit/table-tree" />

## Uncontrolled

The expansion and collapsion of each row is handled automatically.

Use the `isDefaultExpanded` prop to change whether a row is expanded by default.

<Example Component={UncontrolledExample} packageName="@atlaskit/table-tree" />

## Controlled

Use the `isExpanded` prop to manually control whether a row is expanded or not.

Respond to user interaction by providing callbacks to the `onExpand` and
`onCollapse` props.

<Example Component={ControlledExample} packageName="@atlaskit/table-tree" />

## Custom children

You can use other components as children instead of additional rows and items.

<Example Component={CustomChildExample} packageName="@atlaskit/table-tree" />

## Loading states

Each nesting level can be displayed in a loading state. This is useful when
loading data asynchronously.

### Root

To display the root level in a loading state, ensure that the `items` value
provided to `Rows` is `undefined`.

<Example Component={LoadingStateRootExample} packageName="@atlaskit/table-tree" />

### Nested

To display a nested level in a loading state, ensure that the `items` value
provided to the corresponding `Row` is `undefined`.

<Example Component={LoadingStateNestedExample} packageName="@atlaskit/table-tree" />

## Managing data

You can use the `TableTreeDataHelper` class to handle the manipulation of table items,
such as when loading data asynchronously.

### Append items

The `appendItems` method will add to the existing items.

<Example Component={AsyncAppendExample} packageName="@atlaskit/table-tree" />

### Update items

The `updateItems` method will replace the existing items.

<Example Component={AsyncUpdateExample} packageName="@atlaskit/table-tree" />
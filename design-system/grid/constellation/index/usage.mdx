---
order: 2
---

import { BreakpointsTable } from '../usage/breakpoints-table';

## Usage

TODO

## Breakpoints

We have pre-configured breakpoints which the `GridItem`'s `span` and `offset` props are built around.

These breakpoints also modify the number of columns, the gap between columns, and the `Grid`'s padding.

<BreakpointsTable />

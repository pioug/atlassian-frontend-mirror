---
order: 0
---

import SectionMessage from '@atlaskit/section-message';

import MotionFadeOutSingleElement from '../../examples/constellation/motion-fade-out-single-element';
import MotionSlideIn from '../../examples/constellation/motion-slide-in';
import MotionZoomIn from '../../examples/constellation/motion-zoom-in';
import MotionShrinkOut from '../../examples/constellation/motion-shrink-out';
import MotionFadeInListOfElements from '../../examples/constellation/motion-fade-in-list-of-elements';
import MotionFadeInGridOfElements from '../../examples/constellation/motion-fade-in-grid-of-elements';
import MotionFadeBetweenElements from '../../examples/constellation/motion-fade-between-elements';
import MotionFadeOutListOfElements from '../../examples/constellation/motion-fade-out-list-of-elements';

Motion comes with entering motions out of the box.
Most motions have a **pairing exiting motion**, while some only have an exiting motion and no entering.

For consistency, don't try to mix and match.
If an element **enters with one motion** it should **leave with the same motion**.

## Fading in

`<FadeIn />` is useful for fading in one or more elements.

<Example
  highlight="5,19-21"
  packageName="@atlaskit/motion"
  Component={MotionFadeOutSingleElement}
  title="Single element"
/>

## Sliding in

`<SlideIn />` slide an element into position, generally used for things that appear from outside of the viewport into view.

<Example
  highlight="7,37-52"
  packageName="@atlaskit/motion"
  Component={MotionSlideIn}
  title="Slide in element"
/>

## Zooming in

`<ZoomIn />` will over zoom an element into position.

<Example
  highlight="6,20-36"
  packageName="@atlaskit/motion"
  Component={MotionZoomIn}
  title="Zoom in elements"
/>

## Shrinking out

<SectionMessage appearance="warning">
  This motion only has an exiting motion. StaggeredEntrance will have no effect.
</SectionMessage>

`<ShrinkOut />` will shrink an element down to nothing when exiting.
Works best with flex children as collapsing margins can come with undesired behavior.

<Example
  highlight="6,29-57"
  packageName="@atlaskit/motion"
  Component={MotionShrinkOut}
  title="Shrink out elements"
/>

## Staggered entrance

`<StaggeredEntrance />` is useful for staggering an entering motion over many elements.

<Example
  highlight="11,35-38,40,63-66"
  packageName="@atlaskit/motion"
  Component={MotionFadeInListOfElements}
  title="List of elements"
/>

<Example
  highlight="16,68,87,72,85"
  packageName="@atlaskit/motion"
  Component={MotionFadeInGridOfElements}
  title="Staggered entrance with fade in"
/>

## Exiting persistence

`<ExitingPersistence />`is useful for enabling elements to persist and animate away when they are removed from the DOM.

<Example
  highlight="7,79-81,34-45,21-22"
  packageName="@atlaskit/motion"
  Component={MotionFadeBetweenElements}
  title="Single element"
/>

<Example
  highlight="13,50-54,89-92"
  packageName="@atlaskit/motion"
  Component={MotionFadeOutListOfElements}
  title="List of elements"
/>

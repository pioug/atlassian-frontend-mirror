---
title: Country select
description: Country select allows users to make a single selection from a list of countries.
order: 0
---

import SelectCountry from '../../examples/constellation/select-country';

Country select comes with sensible defaults to select a single country from a list of countries.

<Example Component={SelectCountry} packageName="@atlaskit/select" />

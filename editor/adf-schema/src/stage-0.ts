import stageZeroSchema from '../json-schema/v1/stage-0.json';

// eslint-disable-next-line @atlaskit/editor/no-re-export -- This public JSON entry point preserves the legacy default export.
export default stageZeroSchema;

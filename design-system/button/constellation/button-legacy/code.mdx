---
title: Button (legacy)
order: 1
---

import SharedProps from '!!extract-react-types-loader!../../extract-react-types/legacy-button/shared-props';
import LoadingButtonProps from '!!extract-react-types-loader!../../extract-react-types/legacy-button/loading-button-props';
import CustomThemeButtonProps from '!!extract-react-types-loader!../../extract-react-types/legacy-button/custom-theme-button-props';

## Props

### Shared props

These props can be applied to all buttons (`Button`, `LoadingButton` and `CustomThemeButton`).

All buttons also support all valid `HTMLElement` props, except for `disabled` which is replaced with the `isDisabled` prop.

<PropsTable heading="" props={SharedProps} />

### Loading button props

The `LoadingButton` accepts all shared props (except for `overlay`) as well as an optional `isLoading` prop.

<PropsTable heading="" props={LoadingButtonProps} />

### Custom theme button props

The `CustomThemeButton` accepts all shared props (except for `overlay`) as well as an optional `isLoading` prop, and an optional `theme` prop.

<PropsTable heading="" props={CustomThemeButtonProps} />

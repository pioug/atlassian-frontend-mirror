---
order: 0
---

import TagDefault from '../../examples/constellation/tag-default';
import TagLink from '../../examples/constellation/tag-link';
import TagRemovable from '../../examples/constellation/tag-removable';
import TagRemovableLink from '../../examples/constellation/tag-removable-link';
import TagRounded from '../../examples/constellation/tag-rounded';
import TagRoundedLink from '../../examples/constellation/tag-rounded-link';

import TagColors from '../../examples/constellation/tag-colors';
import TagMaxLength from '../../examples/constellation/tag-max-length';

## Default

The default form of a tag, where `text` is required. Tags with static text can be used as a flag or as a reference to an object or attribute.

<Example Component={TagDefault} packageName="@atlaskit/tag" />

## Tag link

A tag with an `href` can link to more information on the tagged item.

<Example Component={TagLink} packageName="@atlaskit/tag" />

## Removable

Once a tag has been removed, it cannot be re-rendered. Removable tags are visible in "edit" mode or in multi-select controls.

<Example Component={TagRemovable} packageName="@atlaskit/tag" />

## Removable link

A removable tag with an `href` can link to more information.

<Example Component={TagRemovableLink} packageName="@atlaskit/tag" />

## Rounded

Rounded tags with an avatar are used to add or remove people in multi-select controls.

<Example Component={TagRounded} packageName="@atlaskit/tag" />

## Rounded link

A rounded tag with an avatar can link to more information.

<Example Component={TagRoundedLink} packageName="@atlaskit/tag" />

## Color

The color theme for background and text.

<Example Component={TagColors} packageName="@atlaskit/tag" />

## Text max length

Once the text reaches 200px, it is truncated with an ellipsis.

<Example Component={TagMaxLength} packageName="@atlaskit/tag" />

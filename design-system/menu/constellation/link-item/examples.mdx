---
title: Link item
description: A menu item wrapped in an anchor tag.
order: 0
---

import LinkItemDefault from '../../examples/constellation/link-item-default';

## Default

The `LinkItem` component renders an item wrapped in an anchor tag `<a>` - useful when wanting to transition to another page. If you need to use a specific router component for route transitions, you'll want to compose them together using `CustomItem`

<Example Component={LinkItemDefault} packageName="@atlaskit/menu" />

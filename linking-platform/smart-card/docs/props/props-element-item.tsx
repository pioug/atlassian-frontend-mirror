import { type ElementItem } from '../../src/view/FlexibleCard/components/blocks/types';

/**
 * Hack to display Flexible Smart Links `ElementItem` for documentation purpose.
 */
export default (props: { props: ElementItem }) => null;

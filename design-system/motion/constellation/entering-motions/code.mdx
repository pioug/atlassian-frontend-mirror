---
order: 1
---

import SectionMessage from '@atlaskit/section-message';

import FadeInProps from '!!extract-react-types-loader!../../src/entering/fade-in';
import SlideInProps from '!!extract-react-types-loader!../../src/entering/slide-in';
import ZoomInProps from '!!extract-react-types-loader!../../src/entering/zoom-in';
import ShrinkOutProps from '!!extract-react-types-loader!../../src/entering/shrink-out';
import StaggeredEntranceProps from '!!extract-react-types-loader!../../src/entering/staggered-entrance';
import ExitingPersistenceProps from '!!extract-react-types-loader!../../src/entering/exiting-persistence';

## <FadeIn />

Useful for fading in one or more elements.

<PropsTable heading="" props={FadeInProps} />

## <SlideIn />

Will slide an element into position, generally used for things that appear from outside of the viewport into view.

<PropsTable heading="" props={SlideInProps} />

## <ZoomIn />

Will over zoom an element into position.

<PropsTable heading="" props={ZoomInProps} />

## <ShrinkOut />

<SectionMessage appearance="warning">
  This motion only has an exiting motion. StaggeredEntrance will have no effect.
</SectionMessage>

Will shrink an element down to nothing when exiting.
Works best with flex children as collapsing margins can come with undesired behavior.

<PropsTable heading="" props={ShrinkOutProps} />

## <StaggeredEntrance />

Useful for staggering an entering motion over many elements.

<PropsTable heading="" props={StaggeredEntranceProps} />

## <ExitingPersistence />

Useful for enabling elements to persist and animate away when they are removed from the DOM.

<PropsTable heading="" props={ExitingPersistenceProps} />

import React from 'react';

// Note: There are a lot of required props without any default, so they are
// included in all examples below to avoid prop warning log noise.
export default {
  isOpen: false,
  onBackButton: () => {},
  backIcon: <span />,
  primaryIcon: <span />,
};

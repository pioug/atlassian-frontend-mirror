---
title: Button (new)
description: A button triggers an event or action. They let users know what will happen next.
order: 1
---

import CommonDefaultButtonProps from '!!extract-react-types-loader!../../extract-react-types/new-button/variants/default/common-default-button-props';
import ButtonProps from '!!extract-react-types-loader!../../extract-react-types/new-button/variants/default/button-props';
import SectionMessage from '@atlaskit/section-message';

<SectionMessage>
  <p>
    Migrate old <tt>@atlaskit/button</tt>s to new button, icon button, or link
    button components using our codemod.{' '}
    <a href="/components/button/migration-guide">View the migration guide</a>
  </p>
</SectionMessage>

## Props

### Common props

These props work for both `Button` and `LinkButton`. Each button type also supports its respective HTML element attributes, **except for**:

- `disabled` (use `isDisabled` instead)
- `style`
- `role`

<PropsTable heading="" props={CommonDefaultButtonProps} />

### Button props

<PropsTable heading="" props={ButtonProps} />

---
order: 0
title: Tag
description: A tag is a subtle, compact label used to categorize and organize content.
---

import SectionMessage from '@atlaskit/section-message';
import TagNewDefault from '../../examples/constellation/tag-new-default';
import TagNewColors from '../../examples/constellation/tag-new-colors';
import TagNewLink from '../../examples/constellation/tag-new-link';
import TagNewRemovable from '../../examples/constellation/tag-new-removable';
import TagNewWithIcon from '../../examples/constellation/tag-new-with-icon';
import TagNewLinkWithIcon from '../../examples/constellation/tag-new-link-with-icon';
import TagNewMaxWidth from '../../examples/constellation/tag-new-max-width';
import TagNewSwatchBefore from '../../examples/constellation/tag-new-swatch-before';
import TagNewTrailingMetric from '../../examples/constellation/tag-new-trailing-metric';
import AddTagExample from '../../examples/15-add-tags';

<SectionMessage title="Motion in Early Access" appearance="warning">
	Motion has been updated in Tag and is in Early Access. The motion updates are currently behind the
	feature flag: platform-dst-motion-uplift-labels.
</SectionMessage>

## Default

The default form of a tag, where `text` is required. Tags with static text can be used as a flag or
as a reference to an object or attribute. By default, tags are removable.

<Example
	Component={TagNewDefault}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Add a tag

Add and remove a tag to see its enter and exit motion.

<Example
	Component={AddTagExample}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['motionLabels']}
/>

## Colors

Tags are available in a range of colors to help you organize and categorize information.

<Example
	Component={TagNewColors}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Link

A tag with an `href` can link to more information on the tagged item.

<Example
	Component={TagNewLink}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Non-removable

Set `isRemovable` to false to prevent users from removing the tag. Tags are removable by default.

<Example
	Component={TagNewRemovable}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## With icon

Use `elemBefore` to add an icon or other element before the tag text.

<Example
	Component={TagNewWithIcon}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Link with icon

Tags can combine both a link and an icon element.

<Example
	Component={TagNewLinkWithIcon}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Max width

Use `maxWidth` to control the maximum width of the tag. When the tag exceeds this width, it will be
truncated with an ellipsis.

<Example
	Component={TagNewMaxWidth}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Trailing metric

Use `trailingMetric` to render a trailing metric after the tag text for compact counts or short
supplementary values. The trailing metric uses background subtler accent tokens.

<Example
	Component={TagNewTrailingMetric}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

## Swatch before

Tags that use color as a wayfinding method (such as Jira epic or parent labels) can leverage the
`swatchBefore` property to increase prominence of that color. Pass `true` to use the tag's own
color, or pass a background design token to use a custom color.

This prop is early access and may be changed in the future.

<Example
	Component={TagNewSwatchBefore}
	packageName="@atlaskit/tag"
	featureFlagSwitchers={['labelling', 'motionLabels']}
/>

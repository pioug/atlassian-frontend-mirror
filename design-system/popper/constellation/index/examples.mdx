---
order: 0
---

import BasicPositioning from '../../examples/constellation/popper-basic-positioning';
import ScrollContainer from '../../examples/constellation/popper-scroll-container';

## Basic positioning

<Example Component={BasicPositioning} packageName="@atlaskit/popper" />

## Scroll container

This example uses the `isReferenceHidden` render prop from the `<Popper />` component to conditionally add styles
as the reference element enters and leaves the viewport.

<Example Component={ScrollContainer} packageName="@atlaskit/popper" />

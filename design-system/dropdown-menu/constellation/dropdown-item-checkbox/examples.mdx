---
title: Dropdown item checkbox
description: A dropdown item checkbox lets users select items from a list of options.
order: 0
---

import DropdownItemCheckboxDefaultSelected from '../../examples/constellation/dropdown-item-checkbox-default-selected';
import DropdownItemCheckboxSelected from '../../examples/constellation/dropdown-item-checkbox-selected';

## Default selected

This makes the checkbox item selected when initially opened.
Use `defaultSelected` when not controlling the checkbox item state.

<Example
  Component={DropdownItemCheckboxDefaultSelected}
  packageName="@atlaskit/dropdown-menu"
/>

## Selected

This makes the checkbox item selected when `true`.
Use `isSelected` when controlling the checkbox item state.

<Example
  Component={DropdownItemCheckboxSelected}
  packageName="@atlaskit/dropdown-menu"
/>

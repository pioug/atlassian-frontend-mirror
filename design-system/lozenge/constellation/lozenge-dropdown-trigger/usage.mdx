---
order: 2
title: Lozenge dropdown trigger
description:
  Lozenge dropdown trigger enables a lozenge to be interactive, and opens a dropdown to update the
  selection.
---

import Image from '@atlaskit/image';

import lozengeDropDownAnatomyLight from '../images/lozenge-dropdown-anatomy-light.png';
import lozengeDropDownAnatomyDark from '../images/lozenge-dropdown-anatomy-dark.png';
import lozengeDropdownTriggerDo from '../images/lozenge-dropdown-do-1-light.png';
import lozengeDropdownTriggerDont from '../images/lozenge-dropdown-dont-1-light.png';
import lozengeDropdownTriggerDoDark from '../images/lozenge-dropdown-do-1-dark.png';
import lozengeDropdownTriggerDontDark from '../images/lozenge-dropdown-dont-1-dark.png';
import lozengeDropdownTriggerDo2Light from '../images/lozenge-dropdown-do-2-light.png';
import lozengeDropdownTriggerDo2Dark from '../images/lozenge-dropdown-do-2-dark.png';
import lozengeDropdownTriggerDont2Light from '../images/lozenge-dropdown-dont-2-light.png';
import lozengeDropdownTriggerDont2Dark from '../images/lozenge-dropdown-dont-2-dark.png';

## Usage

Use a lozenge dropdown trigger when a [lozenge](/components/lozenge/lozenge) needs interactivity. It
shows the current attribute and opens a dropdown so people can switch between related labels.

Lozenge should be used for important labels that need to be easily noticed — like workflow status
(e.g. On track, In progress, Completed), system state (e.g. Live, Active, On, Disabled), priority
(High, P1, Urgent), permissions (Locked, Read only, Private) and promotional labels (Premium, New,
Try). [Tag](/components/tag/tag) should be used instead when the label is just descriptive metadata
that helps classify, group, or filter information.

## Parts

<Image
	src={lozengeDropDownAnatomyLight}
	srcDark={lozengeDropDownAnatomyDark}
	alt="Lozenge dropdown anatomy diagram."
/>

1. **Icon (optional):** Use an icon to add additional affordance where the icon has a clear and
   well-established meaning. For example, arrow up right for "On track".
2. **Label**: Text describing the condition. For example, "On track", "At risk", or "Read only".
3. **Badge (optional)**: Use the `trailingMetric` prop to add a score or metric that supports the
   label.
4. **Dropdown icon**: Indicates that this lozenge has interactivity and can be selected to open a
   menu.

## Best practices

### Use lozenge dropdown trigger to open a dropdown for changing the label

The lozenge dropdown trigger should open a dropdown menu or popup so people can quickly change the
displayed label.

<DoDontGrid>
	<DoDont
		type="do"
		image={{
			url: lozengeDropdownTriggerDo,
			urlDarkMode: lozengeDropdownTriggerDoDark,
			alt: 'Use to enable quick switching between different lozenges in a menu.',
		}}
	>
		Use to enable quick switching between different lozenges in a menu.
	</DoDont>
	<DoDont
		type="dont"
		image={{
			url: lozengeDropdownTriggerDont,
			urlDarkMode: lozengeDropdownTriggerDontDark,
			alt: 'Don’t use it to show additional read-only information.',
		}}
	>
		Don’t use it to show additional read-only information.
	</DoDont>
</DoDontGrid>

### Use the spacing property appropriately

Use the spacing prop to adjust the lozenge dropdown trigger so its size aligns with surrounding
elements, such as when it appears alongside other buttons.

<DoDontGrid>
	<DoDont
		type="do"
		image={{
			url: lozengeDropdownTriggerDo2Light,
			urlDarkMode: lozengeDropdownTriggerDo2Dark,
			alt: 'Use a spacious lozenge dropdown trigger alongside other buttons.',
		}}
	>
		Use a spacious lozenge dropdown trigger alongside other buttons.
	</DoDont>
	<DoDont
		type="dont"
		image={{
			url: lozengeDropdownTriggerDont2Light,
			urlDarkMode: lozengeDropdownTriggerDont2Dark,
			alt: 'Don’t use a default lozenge dropdown trigger alongside other buttons.',
		}}
	>
		Don't use a default lozenge dropdown trigger alongside other buttons.
	</DoDont>
</DoDontGrid>

## Accessibility

- Don't use color alone to signify an important attribute. Instead, use clear labels and, if
  relevant, a supporting icon. For example: for a critical condition, use descriptors like "Off
  track" or "Blocked".
- Don't use long labels in lozenge dropdown triggers. They have a max width of 200 pixels (or a
  custom `maxWidth` value) causing labels to truncate. Ensure truncated text remains accessible via
  the interactive trigger and dropdown menu options.

## Data Center products

For all new features, we recommend using Atlassian Design System and other
[Atlaskit components](https://atlaskit.atlassian.com/). For existing code, continue to use
[Atlassian User Interface (AUI)](https://aui.atlassian.com/).

## Related

- For non-interactive lozenges, use [lozenge](/components/lozenge/lozenge).
- For tallies or scores, use a [badge](/components/badge/badge).
- For descriptive metadata and information, use a [tag](/components/tag/tag).

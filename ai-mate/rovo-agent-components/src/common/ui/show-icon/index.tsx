import React from 'react';

import { type NewCoreIconProps } from '@atlaskit/icon';
import EyeOpenIcon from '@atlaskit/icon/core/eye-open';

export const ShowIcon = (props: NewCoreIconProps) => <EyeOpenIcon {...props} />;

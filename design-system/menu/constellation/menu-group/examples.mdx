---
title: Menu group
description: A generic menu group for rendering menus within navigation elements.
order: 0
---

import MenuDefault from '../../examples/constellation/menu-default';
import MenuDensity from '../../examples/constellation/menu-density';

## Default

Menu group is designed to be placed inline inside of navigation elements.

<Example Component={MenuDefault} packageName="@atlaskit/menu" />

## Density

The menu group can be configured to accept different spacing values. Applying a `compact` spacing value
will trim the gutters and whitespace, for higher density content.

<Example Component={MenuDensity} packageName="@atlaskit/menu" />

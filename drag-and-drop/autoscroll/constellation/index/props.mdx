## API

### autoScroller.start({ input })

Starts monitoring drag input. Call this inside of `onDragStart`.

### autoScroller.stop()

Stops monitoring drag input. Call this inside of `onDrop`.

### autoScroller.updateInput({ input })

Provides drag input data used to determine where to scroll. Call this inside of `onDrag`.
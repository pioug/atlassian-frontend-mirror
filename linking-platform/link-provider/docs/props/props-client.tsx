import type { CardClient as CardClientInterface } from '../../src/client/types';

/**
 * Hack to display prop in details for documentation purpose.
 */
export default (props: CardClientInterface) => null;
